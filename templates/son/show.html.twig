{% extends 'layouts/accueil.html.twig' %}

{% block title %}Son
{% endblock %}

{% block body %}
	<h1>Son</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id du son</th>
				<td>{{ son.id }}</td>
			</tr>
			<tr>
				<th>Nom du son</th>
				<td>{{ son.nomSon }}</td>
			</tr>
			<tr>
				<th>Longueur du son</th>
				<td>{{ son.longueurSon }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('son_index') }}">Retour à la liste des sons</a>

	<a href="{{ path('son_edit', {'id': son.id}) }}">Modifier</a>

	{{ include('son/_delete_form.html.twig') }}
{% endblock %}
